<img class="postImage" src="{{post.image}}" alt="{{post.topic}}"/>
<div class="postContent">
  <h3>{{post.pseudonym}}</h3>
  <p>{{post.topic}}</p>
  <p>{{post.body}}</p>
  {{update-post post=post update="update"}}
</div>
<button {{action 'delete' post}}>Delete</button>
